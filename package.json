{
  "name": "NextJs_NextJs_Keygate",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c green,cyan \"pnpm --filter frontend dev\" \"pnpm --filter backend start:dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.7.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2"
  }
}
